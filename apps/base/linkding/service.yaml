apiVersion: v1
kind: Service
metadata:
  labels:
    app: linkding
  name: linkding-svc
spec:
  ports:
  - port: 9000
    protocol: TCP
    targetPort: 9000
  selector:
    app: linkding
  type: ClusterIP
